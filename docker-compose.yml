services:
  clocktower-assistant:
    build:
      context: .
    image: clocktower-assistant:latest
    container_name: clocktower-assistant
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    restart: unless-stopped
